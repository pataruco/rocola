[package]
name = "rocola-core"
version = "0.0.0"
edition.workspace = true
repository.workspace = true
description = "Domain types and matching engine for rocola. No I/O, no network dependencies."

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
